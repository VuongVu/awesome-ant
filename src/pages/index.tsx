import React from 'react';
import { NextPage } from 'next';

import Layout from 'components/common/layout';

const Home: NextPage = () => {
    return (
        <Layout>
            <div>Home Page</div>
        </Layout>
    );
};

export default Home;
